streamlit==1.31.1          # Patch fix on 1.31, stable and widely used
pandas==2.1.4              # Good pick, last in 2.1.x series
numpy==1.26.4              # Latest patch of 1.26.x (small fix over 1.26.3)
plotly==5.18.0             # Stable, no need to change
psycopg2-binary==2.9.9     # Latest 2.9.x, perfect
python-dotenv==1.0.1       # Small patch over 1.0.0
pytz==2024.1               # Latest stable release, no changes
tabulate==0.9.0            # Add this for your markdown table issue
